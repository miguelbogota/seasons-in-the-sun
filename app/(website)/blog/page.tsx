import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Seasons in the sun | Blog',
  description: 'Game dev blog',
};

export default function BlogPage() {
  return (
    <div>
      <h1>Blog page</h1>
    </div>
  );
}
